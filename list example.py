#  Trying to print the list of names

names = ['Bob', 'Fred', 'George', 'Bill', 'Sally', 'Morgan']


indx = 0
for count in names:  # Counting through the names list
    # Using the index location to print the item at that index
    print(names[indx])
    indx += 1
